<div class="post-card">
    <h3><%= link_to post.title, user_post_path(post.author, post), class: "post-card-title" %></h3>
    <p><%= post.text[0..200] %>...</p>
    <p class="post-info">
        <span>Comments: 💬<%= post.comments_counter || 0 %></span>
        <span>Likes: 💖<%= post.likes_counter || 0 %></span>
    </p>
</div>